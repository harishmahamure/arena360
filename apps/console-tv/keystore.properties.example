# Copy to keystore.properties (gitignored) and place your upload keystore locally.
# CI uses ANDROID_KEYSTORE_* environment variables instead.
storeFile=release/console-tv.jks
storePassword=your-store-password
keyAlias=console-tv
keyPassword=your-key-password
