/**
 * @deprecated Import from `../mui-theme.js` or `@gaming-cafe/theme` instead.
 * This shim exists for transitional imports only; implementation lives in mui-theme.ts.
 */
export {
  colors,
  darkSurfaces,
  muiDarkTheme as darkTheme,
  muiTheme as default,
} from '../mui-theme.js';
